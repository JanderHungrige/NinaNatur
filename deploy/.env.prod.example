# Copy to deploy/.env.prod on the host (never commit the real file).
COMPOSE_PROJECT_NAME=ninanatur-prod
REGISTRY=ghcr.io
OWNER=janderhungrige
IMAGE=ninanatur
IMAGE_TAG=main
APP_PORT=4000
NINANATUR_ENV=prod
