"""Warren Davison's Draft Sketch, converted into the plan's theme (doc 97).

`png` reads his images, `cim` his symbols, `tiles` turns a symbol's fill into
one repeating tile, `outline` turns what it draws along a shape into rules, and
`emit` writes both. `scripts/stylx_to_theme.py` runs them.
"""
